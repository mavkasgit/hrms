[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = module
# Only real test package — never collect backend/scripts/test_*.py (dev utilities).
testpaths = tests
python_files = test_*.py
addopts = --durations=20
markers =
    requires_truncate: force TRUNCATE isolation for this test (when savepoint is insufficient)
